/*
Package mock provides an in-memory mock implementation of the kv.KV interface.

It can be pre-seeded with data, configured with per-operation overrides, and it
records calls for assertions in tests.
*/
package mock

