/*
Package sql provides a client interface for executing SQL operations through
the Tarmac host runtime.

The package defines the surface area for SQL execution and query operations
based on the protobuf definitions, leaving implementation details to follow.
*/
package sql
